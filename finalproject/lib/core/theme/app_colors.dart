import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Modern Palette)
  static const Color primary = Color(0xFF1976D2); // Strong Blue
  static const Color primaryVariant = Color(0xFF1565C0); // Deeper Blue
  static const Color accent = Color(0xFFFFB300); // Vivid Amber
  static const Color accentVariant = Color(0xFFFFA000); // Deeper Amber
  static const Color error = Color(0xFFE53935); // Red
  static const Color warning = Color(0xFFFBC02D); // Yellow
  static const Color success = Color(0xFF43A047); // Green
  static const Color info = Color(0xFF0288D1); // Light Blue

  // Light Theme
  static const Color lightBackground = Color(0xFFF5F7FA); // Light Gray
  static const Color lightSurface = Color(0xFFFFFFFF); // Pure White
  static const Color lightCardBackground = Color(0xFFE3E8EE); // Soft Gray
  static const Color lightMainText = Color(0xFF212121); // Headline
  static const Color lightBodyText = Color(0xFF424242); // Body
  static const Color lightCaptionText = Color(0xFF757575); // Caption
  static const Color lightDivider = Color(0xFFBDBDBD); // Neutral Gray
  static const Color lightDrawerBackground = Color(0xFFFFFFFF);
  static const Color lightAppBarBackground = primary;
  static const Color lightButtonBackground = accent;

  // Remove dark theme colors (not used)
}
